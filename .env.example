# Copy to .env.local and fill in. Never commit the real values.
# Used only for deploying from a developer machine:
#   export $(grep -v '^#' .env.local | xargs) && npx wrangler deploy
CLOUDFLARE_API_TOKEN=
