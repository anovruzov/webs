# Image slots

Drop real photography here using these filenames; components pick them up automatically and fall
back to procedural art when a file is missing. Recommended: JPEG/WebP, 2400px wide for heroes,
1600px for cards, sRGB, under 400 KB each.

(The foundation agent lists the exact filenames below.)
